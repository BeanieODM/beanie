Document:
  INSERT:
    insert_many:
    insert_one:
    insert: insert_one(self)
    create: insert
  FIND:
    find_all:
    find_many:
    find_one:
    find: find_many
    all: find_all
    get: find_one(self)
  UPDATE:
    update_all:
    update: get.update
  REPLACE:
    replace_many:
    replace_one:
    replace: replace_one(self)
  DELETE:
    delete_all:
    delete: get.delete

